## Passo executado
Passo 4 - Implementar uma especificação

## O que foi feito
- Alteração do status da spec `002-maiores-altas-baixas.md` para `in-progress` e depois `done`.
- Adição da tabela `altas_baixas_cache` e funções em `app/database.py`.
- Criação do `app/collectors/altas_baixas.py` para coletar cotas de `brapi.dev` com fallback para `yfinance` para ações mais líquidas.
- Criação de `app/services/altas_baixas.py` para formatação.
- Atualização do template `index.html` e `app/main.py` para exibir as tabelas.
- Testes abrangentes incluídos em `tests/test_altas_baixas.py`.
- Atualização de `CHANGELOG.md` e `docs/STATE.md`.

## PR
Aberto PR `feat: maiores altas e baixas do dia` fechando a issue aplicável / completando a spec 002.

## Verificação
```
ruff check . && ruff format --check .
python -m mypy app
python -m pytest -q
```
Resultado: Todos os checks e testes (8 passed) rodando com sucesso, mock HTTP funcionou perfeitamente.

## Próximo passo provável
Passo 2 (Publicar o que está pendente) se fizer merge, senão Passo 6 ou 7.

## Retrospectiva
- O que nas instruções, skills ou contextos atrapalhou, faltou ou estava errado nesta execução? O `yfinance` requer o uso iterativo do endpoint de chart (por exemplo, buscando múltiplos tickers num loop) que aumenta o risco de rate limits e complexidade para fallback em cestas grandes.
- Melhoria proposta: Avaliar endpoint oficial de múltiplos quotes para o fallback ou persistir caching individual.
