## Passo executado
Passo 4 (Implementar uma especificação)

Motivo: Os passos anteriores não se aplicavam.
- Passo 1: O código estava limpo e passando em todas as verificações.
- Passo 2: Não havia nada pendente de publicação.
- Passo 3: Não havia PRs abertos.
- Havia a especificação `001-ibovespa-hoje.md` com status `ready`.

## O que foi feito
- Criação do banco de dados SQLite em `app/database.py`.
- Criação do coletor de dados em `app/collectors/ibovespa.py` integrando com brapi.dev e Yahoo Finance (fallback).
- Criação do serviço `app/services/ibovespa.py` para formatar os dados.
- Criação do frontend em `app/templates/base.html` e `app/templates/index.html` com Plotly.js.
- Atualização das rotas em `app/main.py`.
- Criação de testes utilizando `respx` para mock das APIs e inserção de fixtures.
- Atualização do `requirements.txt` e `requirements-dev.txt`.
- Atualização de `docs/STATE.md`, `CHANGELOG.md` e `docs/specs/001-ibovespa-hoje.md`.

## PR
Aberto o PR de implementação da spec com o prefixo `feat:`.

## Verificação
```bash
ruff check . && ruff format --check . && python -m mypy app && pytest -q
```
Todos os checks passaram.

## Próximo passo provável
Passo 5 - Imaginar a próxima feature, ou Passo 1 se houver algum erro de execução no coletor.
