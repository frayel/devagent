# Auditoria 2026-09-26

Durante a execução não enfrentei grandes ambiguidades, mas adicionei a verificação das fontes em `Maiores Altas` e `Maiores Baixas` com base na exigência ampla de "Honestidade" para garantir a fonte em todos os números exibidos.

Fiz o checkout do arquivo `data.db` que não deve ser alterado nem salvo, já que apenas arquivos sob `docs/auditoria` e `docs/runs` devem ser incluídos na submissão.
