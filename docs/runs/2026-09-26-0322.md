## Passo executado
Passo 5 (Imaginar a próxima feature)

Motivo: Os passos anteriores não se aplicavam.
- Passo 1: O código estava limpo e passando em todas as verificações.
- Passo 2: Não havia nada pendente de publicação.
- Passo 3: Não havia PRs abertos.
- Passo 4: A especificação `001-ibovespa-hoje.md` estava finalizada (`done`) e não havia nenhuma com `status: ready`.

## O que foi feito
- Leitura de `docs/BACKLOG.md` e `docs/STATE.md`.
- Adicionadas 5 novas ideias no backlog (Alertas de volume, Calendário de balanços, Comparador de ações, Rastreio de carteiras recomendadas, Histórico de dividendos) com estimativa de valor, esforço e risco.
- Backlog ordenado por valor dividido por esforço.
- Criação da especificação `docs/specs/002-maiores-altas-baixas.md` com status `ready` a partir da primeira ideia do backlog.

## PR
Aberto o PR com as adições no backlog e a nova spec sob o prefixo `docs:`.

## Verificação
```bash
ruff check . && ruff format --check . && python -m mypy app && pytest -q
```
Todos os checks e testes passaram (foram feitas apenas alterações em arquivos markdown e texto).

## Próximo passo provável
Passo 4 - Implementar a especificação 002.