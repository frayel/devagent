## Passo executado
Passo 1 (Verificar e corrigir - Tratando issue `producao-incorreta`)

## O que foi feito
- Foi removido o `data.db` que havia sido commitado acidentalmente no repositório, expondo fixtures de testes em produção.
- Foi criado o endpoint `GET /api/snapshot` conforme contrato requerido pela auditoria em `auditoria/README.md`.
- A base de dados e os testes foram adaptados para capturar a origem dos dados (`fonte`), que foi incluída na resposta do `/api/snapshot`.

## PR
Aberto um fix PR referenciando a issue `#11`.

## Verificação
Foram executados:
- `python -m pytest`: 18 tests passed.

## Próximo passo provável
Aguardar aprovação do CI ou ir para o Passo 2 de destravamento de PR (se necessário), para posteriormente pegar novas specs ou issues no Passo 6/7.

## Retrospectiva
- O erro que colocou os dados falsos em produção foi o arquivo SQLite de desenvolvimento local (com os dados coletados de testes) ter sido commitado na branch de `main`. Foi preciso removê-lo (`git rm --cached`).
- Melhoria proposta: Manter a verificação do `.gitignore` rigorosa.
