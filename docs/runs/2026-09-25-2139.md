## Passo executado
Passo 1 · Verificar e corrigir.
(Motivo: Primeiro acesso, o repositório estava sem a estrutura básica definida no `AGENTS.md`. Conforme regra "execute somente o primeiro item aplicável", a execução termina aqui.)

## O que foi feito
Criei a estrutura básica de diretórios e arquivos conforme seção 4 do `AGENTS.md`, incluindo `app/`, `tests/`, `docs/`, `render.yaml`, `CHANGELOG.md` e `.github/workflows/ci.yml`. Configurei um FastAPI básico em `app/main.py` com o endpoint `/healthz` e seu respectivo teste. Criei arquivos vazios de documentação base (`STATE.md`, `BACKLOG.md`).

## PR
docs: inicializa repositorio

## Verificação
Foram executados os comandos:
`ruff check . && ruff format --check .` (Sucesso)
`mypy app` (Sucesso)
`pytest -q` (Sucesso)

## Próximo passo provável
Passo 5 - Imaginar a próxima feature (já que o repositório agora está inicializado e sem specs).
