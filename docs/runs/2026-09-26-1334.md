## Passo executado
Passo 1 (Verificar e corrigir). Foi identificada a issue `#18` ("Produção exibe dados de fixtures de teste") originada da falha no auditor de produção, e tratada a primeira pendência na seção Correções do `docs/BACKLOG.md` referente a "Painel exibe dado de teste em produção (Ibovespa 130.000 pontos)".

## O que foi feito
- `app/database.py` modificado para que o caminho do SQLite seja configurável via variável de ambiente `DATABASE_PATH` (com o padrão mantido como `data.db`).
- Criado o arquivo `tests/conftest.py` com uma `autouse fixture` chamada `setup_db` que usa `monkeypatch` e `tmp_path` para apontar `DATABASE_PATH` para um diretório temporário antes da importação, isolando a base de testes da base de execução principal/local.
- Limpeza dos `test_ibovespa.py` e `test_highlights.py`, removendo as fixtures de deleção em banco real.
- Criado o teste de regressão `test_data_db_not_in_git` para confirmar que `data.db` não está versionado.
- Documentado a variável `DATABASE_PATH` em `README.md` e `docs/context/operacao.md`.

## PR
Aberto o PR do agente com prefixo `fix:` (já que a branch de PRs do Jules gerencia commits da thread).

## Verificação
- Foram executados:
  - `python -m pytest -q tests/`: testes passaram (nenhum teste quebrando base de dados real).
  - `ruff check .`: verificações estáticas de lint passaram.
  - `git ls-files data.db`: Retornou vazio, confirmando que o arquivo não está no índice do git.

## Próximo passo provável
Criar a especificação (ADR) para o "Cron job setup in Render" e configurar a coleta na inicialização da aplicação, além de sanear as implementações de User-Agent, e timeout/retry no Passo 1 (Correções: Fallback e limite do Coletor). O presente relatório cobre a remoção e isolamento do `.db`.

## Retrospectiva
- O erro de ter `data.db` trackeado sem percebê-lo antes, devido à não existência de um isolamento no `tests/conftest.py` até agora.
- Nenhuma. O processo de separação da base de dados de teste ajudou na robustez.