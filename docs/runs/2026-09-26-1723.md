## Passo executado
Passo 1 (Verificar e corrigir). Foi tratada a segunda pendência de correção da seção *Correções* do `docs/BACKLOG.md` chamada "Fallback do Yahoo calcula variação mensal como se fosse do dia". Havia outras prioridades (`deploy-falhou`, `producao-incorreta`), porém todas tratadas em sessões/agentes anteriores.

## O que foi feito
- Em `app/collectors/ibovespa.py`, o método `fetch_yfinance()` foi alterado para buscar o penúltimo `close` (`valid_closes[-2]`) no caso de ter recebido base, ou o `meta.previousClose` (com recuo de segurança), ao invés de buscar a abertura mensal diretamente via `chartPreviousClose`.
- Modificada a fixture `tests/fixtures/yfinance_response.json` para definir `chartPreviousClose: 100000.0` e demonstrar em teste (testando se valida `previous_close == 132000.0`).
- Validado o teste de fallback em `tests/test_ibovespa.py`, acrescentando assert explicitando o `previous_close`.
- Documentada a "Armadilha" de usar `range=1mo` e o valor que ele aponta `chartPreviousClose` dentro de `docs/context/fontes-de-dados.md`.
- Apagada a entrada do bug/correção em `docs/BACKLOG.md`.
- Lançado a correção em `CHANGELOG.md` na seção fix.

## PR
Aberto/modificado via sistema PR (commit do agente), sem prefixo manual mas compila o commit em `fix: Yahoo Finance fallback variação diária via penúltimo candle`.

## Verificação
Foram executados:
  - `python -m pytest -q` localmente; com resultado 100% passed.
  - `ruff check .` localmente, passado.

## Próximo passo provável
Abordar o terceiro item de Correções do backlog: "Coletor de destaques viola as regras de coleta (seção 9)" adicionando o backoff, delay e headers identificáveis para requisições, unificando nos utilitários.

## Retrospectiva
- O que nas instruções atrapalhou? O tamanho e truncagem dos scripts ao tentar ler todo os arquivos no shell e gerar um plano, gerando revisão humana ao ignorar variáveis criadas ou não conseguir ver blocos de assert do pytest que faltavam.
- Nenhuma. O contexto estava claro no backlog.
